# 데이터베이스 쓰기 규칙 — 조회는 그냥 하고, 쓰기는 사람이 연 뒤에만 한다

> 한 줄 요지: **조회(select)는 확인 없이 바로 실행한다. 데이터나 구조를 바꾸는 SQL은 사용자가 그 쿼리를 보고 승인하기 전까지 실행할 수 없다.** 이것은 부탁이 아니라 훅으로 강제되며, 승인은 사용자가 열어 주는 일회용 열쇠 파일로 표현된다.

여기서 "쓰기"란 데이터를 넣거나(`insert`) 고치거나(`update`) 지우거나(`delete`), 표와 제약을 바꾸는(`alter`·`drop`·`create`) SQL 전부를 말한다.

---

## 왜 이 규칙이 있나

Supabase의 SQL 실행 도구(`execute_sql`)는 권한 목록에서 통째로 열려 있다. 조회할 때마다 확인 창이 뜨면 작업이 계속 멈추기 때문이다. 그런데 Claude Code의 권한 규칙은 **도구 하나를 통째로 열거나 통째로 닫는 것**만 할 수 있고, 같은 도구 안에서 "`select`는 되고 `delete`는 안 된다"를 가르지 못한다.

그래서 도구를 열어 두면 조회와 함께 **삭제까지 묻지 않고 실행되는 상태**가 된다. 이 규칙과 그 뒤의 훅은 정확히 그 틈을 메운다.

## 이 환경에서 실제로 확인한 사실 — "확인 요청"은 듣지 않는다

권한 판정에는 통과(`allow`) · 확인(`ask`) · 차단(`deny`) 세 가지가 있다. 원래대로라면 쓰기 쿼리에 `ask`를 걸어 확인 창을 띄우는 것이 자연스럽다. 그런데 이 원격 실행 환경에서 존재하지 않는 표를 지우는 무해한 쿼리로 직접 시험해 본 결과는 다음과 같았다.

| 시험한 것 | 결과 |
|---|---|
| 훅이 호출되는가 | 호출된다 |
| 훅이 `ask`(확인)를 냈을 때 | **확인 창 없이 쿼리가 그대로 실행됐다** |
| 훅이 `deny`(차단)를 냈을 때 | 데이터베이스에 닿기 전에 막혔다 |

즉 이 환경에서 실제로 무언가를 멈춰 세우는 판정은 `deny` 하나뿐이다. 그래서 장치는 "물어본다"가 아니라 "막는다"로 만들어져 있다.

## 장치가 어떻게 동작하나

`.claude/hooks/sql-write-guard.py`가 `execute_sql` 호출 직전마다 쿼리문을 읽고 다음 순서로 판단한다.

1. **읽기만 하는 쿼리인가?** 세미콜론으로 나눈 모든 문장이 조회로 시작하고, 데이터를 바꾸는 단어가 어디에도 없으면 그대로 통과시킨다. 이때 문자열과 주석은 먼저 지우므로, `where action = 'delete'` 같은 멀쩡한 조회는 걸리지 않는다.
2. **아니라면, 열쇠가 있는가?** `.claude/sql-write-unlock` 파일이 있으면 이번 한 번만 통과시키고 **그 파일을 즉시 지운다.** 한 번 쓰면 없어지는 일회용 열쇠다.
3. **열쇠도 없으면 차단한다.** 쿼리는 데이터베이스에 닿지 않는다.

판단이 애매하면 통과가 아니라 차단으로 간다. `explain analyze select ...`처럼 실제로는 읽기만 하는 쿼리가 걸리는 오탐이 있는데, 이것은 의도한 절충이다. 열쇠를 한 번 더 만드는 불편이, 데이터가 조용히 지워지는 사고보다 낫다.

## Claude가 지켜야 할 것 — 열쇠를 스스로 만들지 않는다

**열쇠 파일을 만드는 것은 사용자의 승인을 대신하는 행위다. Claude는 사용자가 그 쿼리를 보고 승인하기 전에는 열쇠를 만들지 않는다.**

이 문장이 이 규칙의 핵심이다. Claude도 파일을 만들 수 있으므로, 기술적으로는 스스로 열쇠를 만들고 통과할 수 있다. 그래서 이 장치가 막는 것은 **악의가 아니라 부주의**다. 쓰기가 조용히 지나가는 일을 없애고, 모든 쓰기를 사용자 눈에 보이는 명시적 단계로 만드는 것이 목적이다.

쓰기가 필요할 때 밟는 순서는 이렇다.

1. 실행하려는 SQL 전문을 사용자에게 보여 주고, 무엇이 어떻게 바뀌는지(영향 받는 행 수, 되돌릴 수 있는지) 함께 말한다.
2. 사용자의 승인을 기다린다.
3. 승인을 받으면 열쇠 파일을 만들고 쿼리를 실행한다. 열쇠는 실행과 동시에 사라지므로, 다음 쓰기에는 다시 승인을 받아야 한다.

## 이 규칙이 덮지 않는 것

이 장치는 `execute_sql` 하나만 본다. 마이그레이션 적용(`apply_migration`)처럼 데이터베이스를 바꾸는 다른 경로는 이 훅을 거치지 않는다. 그런 경로를 쓸 때도 위 세 단계(보여 주기 · 승인 · 실행)를 사람이 지켜야 한다.

---

## 적용 범위

- 이 규칙은 **Supabase 데이터베이스에 연결된 저장소**에 적용된다.
- 단일 원본은 창고(Template-repository)의 `.claude/rules/db-write-permission.md`이며, 실제로 강제하는 장치는 같은 저장소의 `.claude/hooks/sql-write-guard.py`와 `.claude/settings.json`의 `PreToolUse` 설정이다. 규칙만 내려가고 훅과 설정이 따라가지 않으면 강제력이 없는 글만 남으므로, 다른 저장소에 옮길 때는 셋을 함께 옮긴다.
- 훅의 판단이 맞는지 확인하는 시험은 `python3 .claude/hooks/sql-write-guard.test.py`로 돌린다.
